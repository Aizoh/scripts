### Branching ###

After creating a branch and pushing the repo and now downloading the repo on the server to track that branch , on the deployment server run  

```bash

#track and checkout a branch
git checkout -b the_branch origin/the_branch

#chekout the branch
git checkout the_branch

```